package library.randomtester;

import java.util.Random;

/**
 * Created by anatarajan on 11/4/15.
 */
public class TimeRounding {

    public static void main(String[] args) {
/*
        Random random = new Random();
        for(int i = 0; i < 10; i++) {
            long timeNow = random.nextInt(100);
            int roundTo = 50;
            System.out.println("TimeNow: " + timeNow);
            //Rounding to nearest 50
            long roundedTime = roundTo * (timeNow/roundTo);
            System.out.println("RoundedTime: " + roundedTime);
            try {
                Thread.sleep(1000);
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
        }
*/


        int x = -50;
        x = -x;
        System.out.println(Integer.valueOf("2e4"));
//        int n = 30;
//        int length = (int)(Math.log10(n)+1);
//        System.out.println(length);
    }
}
